# Инструкция по использованию системы с FFS аутентификацией

## Запуск системы

### 1. Запуск сервера
```bash
python3 run_server.py
```

### 2. Запуск клиента
```bash
python3 run_client.py
```

## Последовательность действий на сервере

1. **Генерация RSA ключей** - нажмите кнопку "🔑 Генерация RSA ключей" (для шифрования бюллетеней)
2. **Генерация FFS параметров** - нажмите кнопку "🔐 Генерация FFS параметров" (для аутентификации)
   - Сервер сгенерирует два больших простых числа p и q (512 бит каждое)
   - Вычислит модуль n = p * q
   - В консоли будут выведены сообщения о процессе генерации
3. **Создание выборов** - перейдите на вкладку "Выборы" и создайте новые выборы
4. **Запуск сервера** - нажмите "▶ Запустить сервер"
5. **Запуск голосования** - нажмите "🟢 Начать голосование"

## Последовательность действий на клиенте

### Регистрация и аутентификация:

1. **Подключение к серверу** - перейдите на вкладку "Подключение" и нажмите "Подключиться"
2. **Генерация DSS ключей** - перейдите на вкладку "Криптография" и нажмите "Сгенерировать DSS ключи" (для подписи бюллетеней)
3. **Регистрация** - перейдите на вкладку "Регистрация", введите ID и имя, нажмите "Зарегистрироваться"
   - Клиент отправит запрос на регистрацию
   - Сервер вернет параметр FFS n
   - Клиент автоматически сгенерирует FFS ключи (s - секретный, v - публичный)
   - В логах будут сообщения на русском о генерации ключей

4. **Аутентификация** - система автоматически начнет аутентификацию по протоколу FFS:
   
   **Шаг 1**: Клиент создает обязательство
   - Генерирует случайное число r
   - Вычисляет x = r^2 mod n
   - Отправляет x и публичный ключ v серверу
   - В консоли: "=== FFS АУТЕНТИФИКАЦИЯ: Шаг 1 - Создание обязательства ==="
   
   **Шаг 2**: Сервер генерирует вызов
   - Генерирует случайный бит b (0 или 1)
   - Отправляет b клиенту
   - В консоли: "=== FFS АУТЕНТИФИКАЦИЯ: Шаг 2 - Генерация вызова ==="
   
   **Шаг 3**: Клиент формирует ответ
   - Вычисляет y = r * s^b mod n
   - Отправляет y серверу
   - В консоли: "=== FFS АУТЕНТИФИКАЦИЯ: Шаг 3 - Формирование ответа ==="
   
   **Шаг 4**: Сервер проверяет ответ
   - Проверяет: y^2 ≡ x * v^b (mod n)
   - Если проверка успешна, клиент аутентифицирован
   - В консоли: "=== FFS АУТЕНТИФИКАЦИЯ: Шаг 4 - Проверка ответа ==="
   - Результат: "FFS: ✓ ПРОВЕРКА УСПЕШНА!" или "FFS: ✗ ПРОВЕРКА ПРОВАЛЕНА!"

### Голосование:

5. **Голосование** - перейдите на вкладку "Голосование", выберите вариант и нажмите "Проголосовать"
6. **Проверка** - после публикации результатов можно проверить свой бюллетень на вкладке "Проверка"

## Отличия от предыдущей версии

### Было (DSA):
- Аутентификация через цифровую подпись DSA
- Клиент подписывал сообщение аутентификации
- Сервер проверял подпись (хотя фактически не делал)

### Стало (FFS):
- Аутентификация через интерактивный протокол FFS
- Клиент доказывает знание секретного ключа s без его раскрытия
- Четырехэтапный протокол: обязательство → вызов → ответ → проверка
- Все случайные числа генерируются через генератор из dss.py (без использования random)
- Все сообщения о шагах аутентификации на русском языке

## Наблюдение за процессом

Все этапы протокола FFS логируются в консоль с подробными сообщениями на русском языке:
- Генерация простых чисел p и q
- Вычисление модуля n
- Генерация ключей клиента
- Каждый шаг протокола аутентификации
- Результаты проверки

Следите за вкладкой "Логи" на клиенте и сервере для отслеживания всех шагов.
